## Photography-Themed Methods Lesson
Created on Eclipse using Java on January 17, 2025. Uploaded onto Github Saturday Sep 12, 2026. Open in Eclipse or IntelliJ to run MethodsApp.java

# Description
My product is designed for beginners to learn about methods in Java using a photography themed approach. The goal of this is for the user to know and understand what a method is and how to use it.

# Built With

# Features
- Title page: allows user to navigate the concepts, activity, and quiz pages
- Concepts page: informs the user about how to use methods and why it is essential in the present Java world
- Activity page: provides the user an opportunity to test their knowledge by implementing flashcards that will prepare the user for the quiz
- Quiz page: tests the user in what they learned overall about program modules such as Java, static Methods, Class Math, static Fields, declaring methods with multiple parameters, notes on declaring and using methods, method-call stack, Java API packages, random number generation, enumeration, and scope of declaration.

# Major Skills

# Areas of Concern
Some features may need updates for compatibility since this was built over 1 year ago

 The activity page
 * provides the user an opportunity to test their knowledge by implementing flashcards that will effectively prepare
 * the user for the quiz. The user will be well-prepared for the quiz since the flashcard layout and questions are 
 * similiar to the ones on the quiz. Finally, the quiz page will test the user in what they learned overall about and
 * not limited to program modules in Java, static Methods, Class Math, and static Fields, declaring methods with multiple
 * parameters, notes on declaring and using methods, method-call stack, Java API packages, random number generation, 
 * introducing enumeration, and scope of declaration. After completing the quiz, the user will know what a method is and how to 
 * use it. Furthermore, they will do these activities with photography adornments.
 * - Features: Vibrant colors in the title page with different images with related colors. There are also different colored panels 
 *   in concepts page. The bar on the right of the concepts with a home button (leads to the main page) with two arrows. The left 
 *   arrow leads to the previous page while the right arrow leads to the next page.
 * - Major Skills: Using arrays, fileReader (reading images too), fileInput, arrays, arraylist, fields, objects, methods to divide tasks, 
 *   classes to divide tasks, imageIcons, JLayeredPane, JPanel, JButtons, JLabels, static objects, BoxLayout for alignment organization, 
 *   actionListeners, JScrollPanes, and fileInput. 
 * - Areas of Concern: The 'next' button in the activityFrame is still enabled after user has completed the activity. In the quiz frame, 
 * 	 the user can click ten times on the radio buttons to enable the 'submit' button without actually completing the entire quiz. 
 */
